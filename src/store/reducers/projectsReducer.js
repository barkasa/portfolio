const initialState = [];

const projectsReducer = (state = initialState, action) => {
  switch (action.type) {
    // Обработка действий (actions) для ключа "projects"
    default:
      return state;
  }
};

export default projectsReducer;
